controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# JWT Login route (handled by security firewall, not controller)
api_login:
    path: /api/auth/login
    methods: ['POST']
