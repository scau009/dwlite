# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
# Environment: dev, prod, test
APP_ENV=prod
# Generate a random secret: php -r "echo bin2hex(random_bytes(16));"
APP_SECRET=change-this-to-a-random-secret-in-production
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# Format: mysql://username:password@host:port/database?serverVersion=8.0&charset=utf8mb4
# IMPORTANT: Configure your server version correctly
DATABASE_URL="mysql://db_user:db_password@db_host:3306/db_name?serverVersion=8.0&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> redis ###
# Redis connection URL for caching and Messenger
REDIS_URL="redis://redis:6379"
###< redis ###

###> symfony/messenger ###
# Messenger uses REDIS_URL for transport (configured in messenger.yaml)
###< symfony/messenger ###

###> lexik/jwt-authentication-bundle ###
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
# Generate JWT keys: php bin/console lexik:jwt:generate-keypair
# Generate a passphrase: php -r "echo bin2hex(random_bytes(32));"
JWT_PASSPHRASE=change-this-passphrase
###< lexik/jwt-authentication-bundle ###

###> symfony/mailer ###
# Development: smtp://mailhog:1025
# Production (Tencent SES): See SES configuration below
MAILER_DSN=smtp://smtp-host:port
###< symfony/mailer ###

###> symfony/lock ###
# Choose one of the stores below
# postgresql+advisory://db_user:db_password@localhost/db_name
# redis://redis:6379
# flock is filesystem-based (not recommended for production with multiple servers)
LOCK_DSN=flock
###< symfony/lock ###

###> nelmio/cors-bundle ###
# Regular expression for allowed origins
CORS_ALLOW_ORIGIN='^https?://(localhost|yourdomain\.com)(:[0-9]+)?$'
###< nelmio/cors-bundle ###

###> app ###
# Backend URL for verification links in emails
APP_URL=https://api.yourdomain.com
# Frontend URL for redirects after verification
FRONTEND_URL=https://yourdomain.com
###< app ###

###> tencent/cos ###
# Tencent Cloud Object Storage credentials
COS_SECRET_ID=your-cos-secret-id
COS_SECRET_KEY=your-cos-secret-key
# Region: ap-beijing, ap-shanghai, ap-guangzhou, ap-chengdu, etc.
COS_REGION=ap-guangzhou
# Bucket name (without region suffix)
COS_BUCKET=your-bucket-name
# CDN domain (optional, for accelerated access)
COS_CDN_DOMAIN=https://cdn.yourdomain.com
###< tencent/cos ###

###> tencent/ses ###
# Tencent Simple Email Service credentials
SES_SECRET_ID=your-ses-secret-id
SES_SECRET_KEY=your-ses-secret-key
# Region: ap-hongkong, ap-singapore, etc.
SES_REGION=ap-hongkong
# From email address (must be verified in Tencent SES)
SES_FROM_EMAIL=noreply@yourdomain.com
SES_FROM_NAME=DWLite
# Email template IDs (set to 0 to use HTML fallback)
SES_VERIFICATION_TEMPLATE_ID=0
###< tencent/ses ###
