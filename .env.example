# Docker Compose Configuration for Production
# Copy this file to .env and fill in your actual values

###> Docker Registry ###
# Container registry URL (with trailing slash)
# Example: ccr.ccs.tencentyun.com/dwlite/
REGISTRY=ccr.ccs.tencentyun.com/your-namespace/
# Image tag to deploy
TAG=latest
###< Docker Registry ###

###> Application Configuration ###
# Generate a random secret: php -r "echo bin2hex(random_bytes(16));"
APP_SECRET=change-this-to-a-random-secret

# Database connection URL
# Format: mysql://username:password@host:port/database?serverVersion=8.0&charset=utf8mb4
DATABASE_URL=mysql://user:password@host:3306/dwlite?serverVersion=8.0&charset=utf8mb4

# Redis connection URL
REDIS_URL=redis://redis:6379

# Mailer DSN
# Development: smtp://mailhog:1025
# Production (Tencent SES): See backend/.env.example for SES configuration
MAILER_DSN=smtp://smtp-host:port

# Frontend URL for CORS and redirects
FRONTEND_URL=https://yourdomain.com
###< Application Configuration ###

###> Grafana Configuration ###
# Grafana admin password (default: admin)
# Change this in production!
GF_ADMIN_PASSWORD=change-this-password
###< Grafana Configuration ###
